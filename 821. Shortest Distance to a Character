include <stdlib.h>
#include <string.h>
#include <math.h>
int min(int a, int b) {  int* res = (int*)malloc(len * sizeof(int));
    int c_pos = -100000; 
    

    for (int i = 0; i < len; i++) {
        if (s[i] == c) {
            c_pos = i;
        }
        res[i] = i - c_pos;
    }
    c_pos = 100000;
    for (int i = len - 1; i >= 0; i--) {
        if (s[i] == c) {
            c_pos = i;
        }
        res[i] = min(res[i], c_pos - i);
    }
    
    return res;
}

    return (a < b) ? a : b;
}

int* shortestToChar(char* s, char c, int* returnSize) {
    int len = strlen(s);
    *returnSize = len; 
    
  
